# If using windows:
`$ cd ~/APPDATA/LOCAL/`
Then do `$ mkdir nvim`
Then clone this repo there
or 
```
$ mkdir C:\Users\CIIT\appdata\local\nvim
$ git clone https://github.com/howdowrite/myvimrc.git .
```
